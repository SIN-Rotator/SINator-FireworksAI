# File: `agent_toolbox/__init__.py`

Root package init for the SINator Agent Toolbox — a FastAPI-based automation server providing REST endpoints for GMX alias management, Fireworks account creation, API key pooling, and browser lifecycle management.

## Exports

- `core/` — Core services (CDP, GMX, Fireworks, Keychain, Pool)
- `api/` — FastAPI route handlers and schemas
- `start_toolbox.py` — Application entry point
