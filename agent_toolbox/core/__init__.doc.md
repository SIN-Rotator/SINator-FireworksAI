# File: `core/__init__.py`

Package init for `agent_toolbox.core`. Contains all core automation services.

## Exports

- `cdp_client` — Raw CDP WebSocket browser automation
- `config_manager` — GMX + Fireworks credential storage
- `cua_helper` — CUA window detection and interaction
- `fireworks_service` — Fireworks account signup, login, API key
- `gmx_service` — GMX alias management and email reading
- `keychain_store` — macOS Keychain API key storage
- `pool_manager` — API key pool lifecycle and statistics
- `billing_tracker` — Fireworks billing page credit scraper
